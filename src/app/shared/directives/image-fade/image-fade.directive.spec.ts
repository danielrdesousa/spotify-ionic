import { ImageFadeDirective } from './image-fade.directive';

describe('Directive: ImageFade', () => {
  it('should create an instance', () => {
    const directive = new ImageFadeDirective();
    expect(directive).toBeTruthy();
  });
});
